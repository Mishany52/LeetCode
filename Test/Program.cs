using System
public class Solution {
    public string LongestCommonPrefix(string[] strs) {
        Boolean end = true;
        string res;
        for (int i = 0; i < strs[0].Length; i++)
        {
            string curr = Convert.ToString(strs[0][i]);
            string temp = res + curr;

            while(end){
                if(strs)
            }
        }
}